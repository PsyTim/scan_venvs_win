
[project]
name = "scan-venvs-win"
version = "0.1.4"
description = "GUI tool to find and clean up Python venv folders on Windows local drives"
readme = "README.md"
requires-python = ">=3.10.9"
dependencies = []